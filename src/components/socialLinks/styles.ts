import styled from "styled-components/macro";

export const Styles = styled.div`
    position: fixed;
    right: 10px;
    bottom: 10px;

    a {
        margin-right: 10px;
    }

    a:hover {
        img {
            transform: scale(1.25);
        }
    }
`;
